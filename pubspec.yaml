name: dart_valkey
description: Dart client for Valkey (and Redis-compatible servers)
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  mockito: ^5.5.0
  build_runner: ^2.7.0
  test: ^1.19.2
  coolint: ^2.0.1
